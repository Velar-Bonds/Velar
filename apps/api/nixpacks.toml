[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd /app && npm install"]

[phases.build]
cmds = ["npm run build -w packages/types", "npm run build -w apps/api"]

[start]
cmd = "node apps/api/dist/main"
