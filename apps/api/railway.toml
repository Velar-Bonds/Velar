[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && npm install && npm run build -w apps/api"

[deploy]
startCommand = "node dist/main"
healthcheckPath = "/api"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
